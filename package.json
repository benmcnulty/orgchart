{
  "name": "distributed-inference",
  "version": "0.1.0",
  "description": "Local distributed inference dashboard for managing Ollama API sources across a network",
  "type": "module",
  "scripts": {
    "start": "bun server.js",
    "dev": "bun --watch server.js",
    "check": "node --check public/inference-policy.js && node --check public/chat.js && node --check public/app.js && node --check public/markdown.js && node --check server.js",
    "test": "bun test",
    "test:unit": "bun test"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
